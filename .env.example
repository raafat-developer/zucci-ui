# Zucci Admin — Environment Variables
# In dev, requests to /api/v1/* are proxied to the real backend by Vite (see vite.config.js).
# In production, set this to the full URL: https://api.zucci.xyz/api/v1/
VITE_API_BASE=/api/v1/
